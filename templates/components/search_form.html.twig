{% set action = action ?? '' %}
{% set placeholder = placeholder ?? 'Rechercher....' %}

<form actionaction="{{ path('app_admin_product_index') }}" method="get">
    {% include 'components/search_field.html.twig' with {
        name: 'q',
        placeholder: placeholder,
        class: class|default('w-96'),
        value: app.request.query.get('q', '')
    } %}
</form>
